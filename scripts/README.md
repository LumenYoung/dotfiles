Here stores the scripts (for now only python) I want to use for some of the machine.

## Usage

Need to manually add this folder into `PATH`.

```fish
set -gx PATH <location of scripts/ folder> $PATH
```

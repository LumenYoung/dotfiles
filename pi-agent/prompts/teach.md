---
description: Start or continue a source-grounded teaching workspace across sessions
argument-hint: "[topic, learning goal, or next-lesson request]"
skill: teach
---

Use the loaded `teach` workflow for the optional request below. If it is empty, inspect the existing teaching workspace and choose the next appropriate learning step.

$@

The Pi installation's `SKILL_ROOT` is `~/.pi/agent/skills/teach`. Treat it as read-only skill material. Confirm the exact `WORKSPACE_ROOT` before creating a new teaching workspace or writing any course artifacts; never write course files under `SKILL_ROOT`. For an existing, unambiguous teaching workspace, continue from its recorded state.
